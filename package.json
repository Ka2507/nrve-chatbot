{
  "name": "nrve-fullstack",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c auto \"npm run dev -w client\" \"npm start -w server\"",
    "install:all": "npm install -w client && npm install -w server",
    "build": "npm run build -w client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}